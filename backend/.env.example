# ================================
# CyberCheck Backend — Ortam Değişkenleri (Örnek)
# Bu dosyayı .env olarak kopyalayın ve değerleri doldurun.
# ================================

NODE_ENV=development
PORT=5000

DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=
DB_NAME=cybercheck_db

JWT_SECRET=
JWT_EXPIRES_IN=7d
